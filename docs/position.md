# Position

```css

/* Font Size */
.p-{breakpoint}-{value}
.p-{sm (small) | md (medium) | lg (large) | blank)-{static | a (absolute) | f (fixed) | r (relative) | sticky | i (initial) }

.p-a === position: absolute
.p-sm-r === position: relative /* (only on small screens) */

```