# Display

```css

.d-{breakpoint}-{value}
.d-{sm (small) | md (medium) | lg (large) | blank)-{i (inline) | b (block) | f (flex) | g (grid) | ib (inline-block) | t (table) | tc (table-cell) | n (none)}

.d-md-f === display: flex (only on medium screens)

```